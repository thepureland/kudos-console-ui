{
  "name": "kudos_console_ui",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webApp"
  ],
  "scripts": {
    "build": "cd webApp && npm run build",
    "build:sys": "cd webApp && npm run build:sys",
    "start": "cd webApp && npm run start",
    "start:sys": "cd webApp && npm run start:sys"
  }
}
