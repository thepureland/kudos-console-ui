import type { PageContext, PageProps } from './pageTypes';

/** Add/Edit dialog props (reuse page-common props). */
export type AddEditDialogProps = PageProps;
/** Add/Edit dialog context (reuse page-common context). */
export type AddEditDialogContext = PageContext;
