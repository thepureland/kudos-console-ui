# 默认模式：带 user 的应用，需要登录鉴权
VITE_REQUIRE_AUTH=true

# 开发时 API 代理目标（前端请求同源，由 Vite 转发到此后端地址）
VITE_API_PROXY_TARGET=http://localhost:8080

# 直连后端，绕过 Vite 代理（前后端端口不同时，若代理导致请求慢可启用；后端需配置 CORS）
# VITE_API_DIRECT=true
